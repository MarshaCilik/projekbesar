alat_sewa.alat_sewa_id (integer)
alat_sewa.nama_alat (character varying)
alat_sewa.stok (integer)
alat_sewa.harga_sewa_perhari (numeric)
alat_sewa.harga_denda_perhari (numeric)
alat_sewa.kategori_id (integer)
alat_sewa.added_at (timestamp without time zone)
barang.barang_id (integer)
barang.nama_barang (character varying)
barang.stok (integer)
barang.harga_per_item (numeric)
barang.kategori_id (integer)
barang.added_at (timestamp without time zone)
detail_transaksi_pembelian.detail_transaksi_pembelian_id (integer)
detail_transaksi_pembelian.quantity (integer)
detail_transaksi_pembelian.harga_per_item (numeric)
detail_transaksi_pembelian.transaksi_id (integer)
detail_transaksi_pembelian.barang_id (integer)
detail_transaksi_sewa.detail_transaksi_sewa_id (integer)
detail_transaksi_sewa.quantity (integer)
detail_transaksi_sewa.tgl_sewa (date)
detail_transaksi_sewa.tgl_pengembalian (date)
detail_transaksi_sewa.harga_sewa_perhari (numeric)
detail_transaksi_sewa.denda (numeric)
detail_transaksi_sewa.transaksi_id (integer)
detail_transaksi_sewa.alat_sewa_id (integer)
detail_transaksi_sewa.status_sewa (character varying)
detail_transaksi_sewa.opsi_pengembalian_id (integer)
kurir.kurir_id (integer)
kurir.nama_kurir (character varying)
kurir.no_telp (character varying)
kurir.alamat (text)
status_distribusi.status_distribusi_id (integer)
status_distribusi.status_distribusi (character varying)
transaksi.transaksi_id (integer)
transaksi.created_at (timestamp without time zone)
transaksi.catatan (text)
transaksi.ongkos_kirim (numeric)
transaksi.users_id (integer)
transaksi.kurir_id (integer)
transaksi.status_transaksi (character varying)
transaksi.status_distribusi_id (integer)
transaksi.metode_pembayaran (character varying)
transaksi.status_pembayaran (character varying)
